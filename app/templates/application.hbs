     <nav>
      {{#link-to 'index'}}Home{{/link-to}}
      {{#link-to 'contact'}}Contacts{{/link-to}}
      {{#link-to 'about'}}About{{/link-to}}
      </nav>
      {{#each}}
      <h3>{{firstName}} {{lastName}}</h3>
      {{/each}}
    {{outlet}}
    <form>
      {{input type="text" value=inputFirstName placeholder="First Name"}}
      {{input type="text" value=inputLastName placeholder="Last Name"}}
      <button {{action 'createPerson'}}>Add Person</button>

    </form>
      You are adding <strong>{{inputFirstName}} {{inputLastName}}</strong> to the best address book of all time!
